using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CSHARP_Console_Write
{
    class Welcome1
    {
        static void Main(string[] args)
        {
            //  (1)  Console.WriteLine("Welcome\nto\nC#\nProgramming\n"); //writes message to console.
            
            //  (2)  Console.WriteLine("{0}\n{1}","Welcome to","C# Programming"); //writes message to console with new lines.
            
            //  (3)  int number1; //variable for first number.
            //int number2; //variable for second number.
            //int sum; //variable to store sum.
            //Console.Write("Enter first integer: "); //writes message prompting user for first number.
            //number1 = Convert.ToInt32(Console.ReadLine());  //takes user input and stores in variable number1.
            //Console.Write("Enter second Integer: ");  //writes message prompting user for second number.
            //number2 = Convert.ToInt32(Console.ReadLine());  //takes user input and stores in variable number1.
            //sum = number1 + number2;  //stores sum of number1 and number2 into sum variable.
            //Console.WriteLine("Sum is {0}", sum);  //writes the sum of the integers to the console.
            
            //  (4)  int number1; //variable for first number.
            //int number2; //variable for second number.
            //Console.Write("Enter first integer: "); //writes message prompting user for first number.
            //number1 = Convert.ToInt32(Console.ReadLine());  //takes user input and stores in variable number1.
            //Console.Write("Enter second Integer: ");  //writes message prompting user for second number.
            //number2 = Convert.ToInt32(Console.ReadLine());  //takes user input and stores in variable number1.
            //if (number1 == number2)  //checks if numbers are equal
            //    Console.WriteLine("{0} == {1}", number1, number2);  //writes that the numbers are equal
            //if (number1 != number2)  //checks if numbers are not equal
            //    Console.WriteLine("{0} != {1}", number1, number2);  //writes that the numbers are not equal  
            //if (number1 > number2)  //checks if number1 is greater than number2
            //    Console.WriteLine("{0} > {1}", number1, number2);  //writes that number1 is greater than number2
            //if (number1 < number2)  //checks if number1 is less than number2
            //    Console.WriteLine("{0} < {1}", number1, number2);  //writes that number1 is less than number2
            //if (number1 <= number2)  //checks if number1 is less than or equal to number2
            //    Console.WriteLine("{0} <= {1}", number1, number2);  //writes that number1 is less than or equal to number2
            //if (number1 >= number2)  //checks if number1 is greater than or equal to number2
            //    Console.WriteLine("{0} >= {1}", number1, number2);  //writes that number1 is greater than or equal to number2
            }
    }
}
